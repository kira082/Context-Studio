{
  "name": "@repo/context-studio",
  "version": "1.0.0",
  "description": "Plug-and-play Context Studio memory module for the No-Code Agent Builder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "jest",
    "db:push": "prisma db push",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "lru-cache": "^10.2.2"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prisma": "^5.15.0",
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  }
}
